export interface User {
    user: string;
    name: string;
    address: string;
    pin: string;
    asked: string;
    location: Location;
  }
  
 export interface Location {
    lat: string;
    lon: string;
  }
